# 开发备注（协作约定）

本项目默认流程：
1. 先在本地修改并验证效果（UI/功能确认没问题）。
2. **只有你明确说“可以提交 / commit & push”**，我才会执行 `git add` / `git commit` / `git push`。
3. 不提交任何敏感信息（例如 `.env.local`、密钥、Token、数据库密码等）。

如果你希望我在某个阶段创建 PR 或者做版本标签（tag/release），你直接说目标即可。

